# Data Storage Directory

This directory stores uploaded and processed data files as well as generated vector indexes.

## Subdirectories:
- `raw/`: Raw uploaded documents (PDF, DOCX, TXT, CSV).
- `processed/`: Parsed text, extracted pages, clean tokens, and extracted metadata JSONs.
- `embeddings/`: Saved FAISS index files and vector embeddings metadata.
